package s.app;

/**
 * Alias for the platform display type provided by Kha.
 *
 * Use this when engine code needs to reference monitor or display information
 * without importing `kha.Display` directly.
 */
typedef Display = kha.Display;
