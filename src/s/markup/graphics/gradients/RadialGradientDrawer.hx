package s.markup.graphics.gradients;

import s.markup.elements.gradients.RadialGradient;

@:allow(s.markup.elements.gradients.RadialGradient)
class RadialGradientDrawer extends GradientDrawer<RadialGradient> {
	function new() {
		super("gradient_radial");
	}
}
