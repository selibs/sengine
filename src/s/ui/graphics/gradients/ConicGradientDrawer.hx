package s.ui.graphics.gradients;

import s.ui.gradients.ConicGradient;

@:allow(s.ui.gradients.ConicGradient)
class ConicGradientDrawer extends GradientDrawer<ConicGradient> {
	function new() {
		super("gradient_conic");
	}
}
